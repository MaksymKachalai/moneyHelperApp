// const { User } = require("../../models/userModel");

// const userLogout = async (req, res) => {
//   const { user } = req;
//   const customer = await User.findByIdAndUpdate(user._id, { token: null }, { returnDocument: 'after' });
//   res.json({
//     userId: customer._id,
//     message: 'User logged out successfully',
//   });
// };

// module.exports = {
//   userLogout,
// };
